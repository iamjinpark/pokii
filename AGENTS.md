# AGENTS.md

이 파일은 **Codex 코드 리뷰 전용**이다. 프로젝트의 기준 문서는 `CLAUDE.md`이고,
제품 결정·도메인 불변식·기술 결정은 전부 거기 있다. 여기에 그 내용을 복사하지 않는다.

## 리뷰 설정

Codex 설정(저장소 밖)에서 Automatic reviews를 켜서 PR마다 리뷰가 돌게 한다.
`@codex review` 코멘트로도 직접 부를 수 있다.

## Code Review Rules

리뷰 전에 `CLAUDE.md`를 읽는다. 아래 규칙은 그 문서의 불변식 중 코드에서 실제로
깨지는 것들이다. 스타일·포맷·네이밍은 지적하지 않는다. ESLint와 CI가 이미 본다.

### 날짜

날짜는 기기 로컬 자정을 경계로 클라이언트가 계산해 서버에 넘긴다. 서버에서
`now()`, `current_date`, `CURRENT_TIMESTAMP`로 날짜를 만드는 코드는 시간대가 다른
기기에서 하루가 어긋난다. 클라이언트가 보낸 날짜를 쓴다.

UTC로 날짜를 자르는 코드도 같은 버그다. `toISOString().slice(0, 10)`은 로컬 자정이
아니라 UTC 자정을 경계로 삼는다.

### 포도알

포도알은 목표당 하루 하나다. 소급 보충은 어제 하루까지만 되고 미래 날짜는 불가다.
범위 검사가 없거나 한쪽만 있는 코드를 지적한다.

유예일은 `started_on + 10`이다. 이 값을 다른 식으로 다시 계산하는 곳이 생기면
두 계산이 어긋날 수 있으니 지적한다.

용기 등급은 저장하지 않고 알 개수로 계산한다(`containerFor(filledCount)`). 등급을
컬럼이나 상태에 들고 있으면 알 개수와 어긋난다.

### 목표

동시 진행 목표는 최대 3개다. 이 상한을 클라이언트에서만 막고 서버(RLS·제약)에서
막지 않으면 우회된다.

기분은 필수다(excited·happy·calm·tired·sad). 선택으로 처리하거나 기본값을 넣는
코드를 지적한다.

### 보안

Supabase RLS 정책에 소유권 검사(`auth.uid()`)가 빠진 곳을 지적한다. 저장소가
공개이므로 스키마는 누구나 읽을 수 있고, 데이터를 막는 건 RLS뿐이다.

사용자가 바뀔 때 남는 캐시나 상태를 지적한다. 로그아웃 후 이전 사용자의 데이터가
다음 사용자에게 보이면 안 된다.

토큰·키가 로그나 에러 메시지에 찍히는 곳을 지적한다.

### 구조

`src/app`은 라우트 전용이다. 화면 구현이 들어가 있으면 지적한다.

전역 상태 라이브러리를 쓰지 않는다. 서버 상태는 TanStack Query, UI 상태는 React
로컬 상태다.

인증은 OAuth 전용이다. 이메일·비밀번호 경로가 생기면 지적한다.

### 이음매

이번 PR이 고친 함수의 다른 호출부가 깨지는지 본다. 같은 규칙을 쓰는 다른 곳을
한쪽만 고친 경우를 지적한다.

### 태도

확신할 수 없으면 추측이라고 쓴다. 문제가 없으면 없다고 쓴다. 억지로 찾지 않는다.
