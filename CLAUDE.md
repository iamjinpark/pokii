# pokii

목표를 정하고 매일 하나씩 포도알을 채워 포도송이를 완성하는 셀프 보상 앱.

상세 설계는 `docs/superpowers/specs/2026-09-03-pokii-v1-design.md`.
아래는 **이미 결정된 것**이다. 대화 중 나온 아이디어로 바꾸지 않는다.
바꿀 일이 생기면 먼저 스펙을 고치고, 이 파일을 함께 고친다.

## 변경 불가

- `applicationId` = `com.pokii.app` (출시 후 변경 시 다른 앱이 됨)
- 표시 이름 = POKII

## 도메인 불변식

- 송이 = **연속 10일 10알**. 칸은 날짜이고, 놓친 날의 칸은 영구히 빈다
- 하루 경계 = **기기 로컬 자정**. 날짜는 클라이언트가 계산해 서버에 전달한다 (`now()` 사용 금지)
- 포도알은 목표당 하루 하나. 소급 보충은 **어제 하루까지만**, 미래는 불가
- 유예일 = `started_on + 10`. 그날까지 10일차 칸을 보충할 수 있고, 다음날 종료
- 동시 진행 목표 **최대 3개**. 목표 생성 시 1번 송이가 그날 시작일로 함께 생성된다
- 기분은 **필수** (5종: excited·happy·calm·tired·sad). 메모는 100자 이내 선택
- 종료 경로는 **끝내기 / 한 송이 더** 둘뿐. 중간 포기 기능 없음
- 용기 등급은 저장하지 않고 알 개수로 계산한다 (`containerFor(filledCount)`)

## 기술 결정

Expo(RN) + expo-router + TS · Supabase(Postgres/Auth/RLS) · TanStack Query ·
reanimated · expo-notifications(로컬) · EAS · 영어/한국어

- **전역 상태 라이브러리 없음.** UI 상태는 React 로컬 상태
- 인증은 **OAuth 전용** (Google, Kakao). 이메일/비밀번호 없음
- `src/app`은 라우트 전용. 화면은 `screens/`, 테스트는 콜로케이트

## v1 제외

결제, 통계·기록 화면, 다크모드, 위젯, 소셜 공유, 목표 공유, 오프라인 지원,
iOS 출시, Apple 로그인, mood 색 팔레트 선택, 목표 개수 확장

## 작업 규칙

- **이 파일이 이 프로젝트의 유일한 기준이다.** 대화 중 나온 아이디어는
  사용자가 이 파일 변경을 명시적으로 승인하기 전까지 요구사항이 아니다
- 이 파일과 충돌하는 요청은 **구현하지 않고 충돌 지점을 먼저 알린다**
- 새 아이디어는 현재 작업에 반영하지 않고 `docs/backlog.md`에만 기록한다.
  Backlog 항목은 사용자가 승인해 Issue가 될 때까지 구현 대상이 아니다

## 대화 모드

기획·탐색·결정·구현을 한 대화에서 섞지 않는다. 사용자가 모드를 선언하고,
모드마다 **산출물이 다르다.** 선언이 없으면 실행 모드다.

| 모드 | 전환 | 하는 일 | 산출물 |
|---|---|---|---|
| 탐색 | `/idea` | 아이디어를 자유롭게 비교. **아무것도 확정하지 않는다** | `docs/backlog.md` (승인 시에만 기록) |
| 결정 | `/decide` | 탐색 결과를 이 파일 기준으로 검토·분류 | GitHub Issue + 이 파일 수정 |
| 실행 |  기본값 | 확정된 것만 구현 | 코드 + PR |

- **탐색 모드** — 구현안이나 최종 설계를 확정하지 않고 선택지만 비교한다.
  코드를 쓰지 않는다
- **결정 모드** — 각 제안을 `핵심 목적에 필요` / `나중에 추가 가능` /
  `현재 목적에서 벗어남`으로 분류한다. **사용자가 명시적으로 선택하기 전까지
  이 파일을 변경하지 않는다.** 선택된 것만 Issue가 된다
- **실행 모드** — 새 기능·UX 개선·구조 변경을 제안하지 않는다.
  구현 중 아이디어가 떠오르면 작업에 반영하지 말고 Backlog에 한 줄만 남긴다
